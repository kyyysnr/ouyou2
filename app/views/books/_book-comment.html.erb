<% @book_comments.each do |comment| %>
<table>
  <tr>
    <td>
      <%= image_tag comment.user.get_profile_image, size:'50x50' %>
    </td>
    <td><%= comment.text %></td>
    <td>
      <% if comment.user == current_user %>
        <%= link_to "Destroy", book_book_comment_path(comment), method: :delete, class: "btn btn-sm btn-danger" %>
      <% end %>
    </td>
  </tr>
</table>

<% end %>

<%= form_with model: [@book,@book_comment], local: false do |f|  %>
  <div class="form-group">
    <%= f.label :text %>
    <%= f.text_area :text, class: 'form-control book_title' %>
  </div>
  <div class="form-group">
    <%= f.submit "送信" ,class: 'btn btn-success' %>
  </div>
<% end %>